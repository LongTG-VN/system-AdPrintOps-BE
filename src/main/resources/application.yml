server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: system-adprintops-be

  datasource:
    url: ${DB_URL:jdbc:postgresql://db.kedseyelxebrcwyvpzts.supabase.co:5432/postgres?sslmode=require}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:UKZRVcw2OepSKBpj}
    driver-class-name: ${DB_DRIVER:org.postgresql.Driver}

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${GMAIL_USERNAME:gialong.game@gmail.com}
    password: ${GMAIL_APP_PASSWORD:xgvsgivhxrpjlcsu}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# Custom Application JWT Configuration
app:
  jwt:
    secret: ${JWT_SECRET:MDEyMzQ1Njc4OTAxMjM0NTY3ODkwMTIzNDU2Nzg5MDEyMzQ1Njc4OTAxMjM0NTY3ODkwMTIzNDU2Nzg5MDEyMw==}
    expiration-ms: 86400000 # 24 hours in milliseconds
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}
  pricing:
    allow-fallback: ${PRICING_ALLOW_FALLBACK:true}
